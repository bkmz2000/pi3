services:
  pi3:
    image: ghcr.io/bkmz2000/pi3:latest
    container_name: pi3
    restart: unless-stopped
    ports:
      - "8080:3001"
    environment:
      - HOST=0.0.0.0
      - PORT=3001
      - DB_PATH=/app/db/pi3.db
    volumes:
      - pi3-db:/app/db

volumes:
  pi3-db:
    driver: local